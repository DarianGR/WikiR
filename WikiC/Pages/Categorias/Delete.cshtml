@page
@model WikiC.Pages.Categorias.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Categoria</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Categoria.NombreCat)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Categoria.NombreCat)
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="Categoria.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>

<style type="text/css">
    body {
        color: #f5f5f5;
        background: radial-gradient(circle, rgba(199,199,199,1) 0%, rgba(90,89,89,1) 45%, rgba(0,0,0,1) 100%);
        font-family: fantasy;
    }

    h1 {
        text-shadow: black 0.1em 0.1em 0.2em;
    }

    .btn {
        margin-top: 40px;
        margin-right: 50px;
        background-image: -webkit-linear-gradient(top, #191919, #bababa);
        border-radius: 5px;
        text-shadow: 0 0 0.1em #bababa;
    }

        .btn:hover {
            background-image: -webkit-linear-gradient(top, #bababa, #191919);
        }

    .control-label {
        text-shadow: black 0.1em 0.1em 0.2em;
        font: condensed 120% fantasy;
        color: #bababa;
    }

    .form-control {
        font: condensed 120% sans-serif;
    }
</style>